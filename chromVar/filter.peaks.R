## input: 1. peaks x (cell + N)
## filter: 1. peaks < min.counts
##         2. overlapped peaks with smaller count


require(data.table)
output.peaks.by.cells.count<- fread("output.peaks.by.cells.count.csv")










